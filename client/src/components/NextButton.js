import React from 'react';
import './NextButton.css';

const NextButton = () => {

    return (
        <div className="survey-next">
            <button className="survey-next-button">Take the survey</button>
        </div>
    );
  
};

export default NextButton;
